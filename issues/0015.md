# 0015 — Jump to top and bottom: g / G and Home / End

| | |
|---|---|
| **Status** | resolved |
| **Module** | Keys / Pager |
| **Platform** | macOS |
| **First seen** | 2026-06-10 |
| **Closed** | 2026-06-10 |
| **Commit** | 7d4daff |

## Description

`less`'s most-used navigation after paging is the leap to either end: `g` (or Home) jumps to the first line, `G` (or End) to the last. NeoPager has line and page movement (#0006) but no way to jump straight to the top or bottom of a large buffer.

## Expected behavior

- `g` and Home → scroll to the very top (offset 0); `atTop` true.
- `G` and End → scroll to the bottom (offset `maxOffset`); `atEnd` true, status bar shows `END`.
- Both are no-ops (no repaint) when already at that end.

## Notes

`g`/`G` are printable characters, so they arrive as `.char("g")` / `.char("G")` in the key handler and route to new `PagerState.scrollToTop()` / `scrollToBottom()` methods (clamped, emit `objectWillChange` only on a real move — same pattern as the existing movement methods).

Home/End are escape sequences and need `RawKeyParser` additions: Home = `ESC[H` or `ESC[1~` (and SS3 `ESC O H`), End = `ESC[F` or `ESC[4~` (and SS3 `ESC O F`). Add `.home` / `.end` events and map them to the same scroll-to-top/bottom actions.

Phase-2 caveat (shared with #0013/#0014): `g`/`G` are literal query characters in search-input mode (#0010) — the jump bindings apply only in normal mode.

## Relation

- Pairs with: [#0006](0006.md)
- Same key-handler change as: [#0013](0013.md), [#0014](0014.md)

## Fix

`PagerState.scrollToTop()` (`setOffset(0)`) and `scrollToBottom()`
(`setOffset(maxOffset)`), both clamped and emitting `objectWillChange` only on a real
move. The key handler routes `.char("g")` and `.home` to `scrollToTop()`, and
`.char("G")` and `.end` to `scrollToBottom()`. `RawKeyParser` gained `.home`/`.end`
events covering CSI `ESC[H`/`ESC[F`, the VT220/rxvt tilde forms `ESC[1~`/`ESC[7~`
(Home) and `ESC[4~`/`ESC[8~` (End), and SS3 `ESC O H`/`ESC O F`.

## Verification

`swift test` (46 total): `PagerState` tests assert scrollToBottom→`atEnd`,
scrollToTop→`atTop`, and no-ops at the ends; `RawKeyParser` tests assert Home/End
decode in all three forms. End-to-end under a pty harness: `G` and the End key →
status shows `END`; `g` and the Home key → `0%`.

## Files changed

- `Sources/NeoPager/PagerState.swift` — `scrollToTop()` / `scrollToBottom()`.
- `Vendor/SwiftTUI/Sources/SwiftTUI/RunLoop/RawKey.swift` — `.home`/`.end` events + decoding.
- `Sources/NeoPager/NeoPager.swift` — `g`/`G`/Home/End bindings.
- `Tests/NeoPagerTests/{PagerStateTests,RawKeyParserTests}.swift` — coverage.

## Gotchas

Home/End have many encodings; accept the CSI letter forms, the numbered tilde forms,
and SS3 — terminals vary (and a `1;<mod>` parameterized Home, e.g. Ctrl-Home, currently
falls through to nil, which is fine). Cross-terminal confirmation of the physical
Home/End keys is the user's eyes-on check for `closed`.
